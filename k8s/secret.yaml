apiVersion: v1
kind: Secret
metadata:
  name: flow-reader-secrets
  namespace: default
type: Opaque
stringData:
  # Sentry (Optional - leave empty if not using error tracking)
  # SENTRY_DSN: ""
  # SENTRY_ORG: ""
  # SENTRY_PROJECT: ""
