apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flow-reader-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # Uncomment and adjust based on your cluster's storage class
  # storageClassName: standard
